---
metaLinks:
  alternates:
    - >-
      https://app.gitbook.com/s/V4pJG7Rv7yabqyiVt2OZ/documentation/error-messages
---

# Error Messages

&#x20;

<table><thead><tr><th valign="top">Error Message</th><th valign="top">Meaning</th></tr></thead><tbody><tr><td valign="top">The source file you entered could not be found</td><td valign="top">User has entered a path/file name which cannot be located.</td></tr><tr><td valign="top">Unable to create the requested file. Make sure the file name is valid and does not contain any illegal characters such as slashes, wild card symbols, etc...</td><td valign="top">This error indicates a server side problem. User actually should never see it.</td></tr><tr><td valign="top"><p>Unable to complete copying the file.</p><p>File System Internal Error</p></td><td valign="top">This error would indicate a server side problem. User actually should never see it.</td></tr><tr><td valign="top">The source file contains no content</td><td valign="top">The file entered by the user contains no data or the data is in a format unidentifiable.</td></tr><tr><td valign="top">A temporary file could not be created</td><td valign="top">This error would indicate a server side problem. User actually should never see it.</td></tr><tr><td valign="top">Error in row: %%1. The row does not have the correct number of columns</td><td valign="top">Typically, this will be generated when TAB-delimited file is loaded and there are too many columns. Check the file, and see if there is a 4<sup>th</sup> column. If you can’t see one, there still may be columns because there were extra TABs in the data.</td></tr><tr><td valign="top">A database connection could not be established</td><td valign="top">This error would indicate a server side problem. User actually should never see it.</td></tr><tr><td valign="top">An Excel error occurred during processing</td><td valign="top">This message would occur during loads from an Excel sheet. Something in the spreadsheet is causing the loader to choke. Maybe a bad formula, etc.</td></tr><tr><td valign="top">Excel could not be accessed</td><td valign="top">This error would indicate a server side problem. User actually should never see it.</td></tr><tr><td valign="top">There are not enough rows in the file.&#x3C; br>There should be 2 header rows and at least one row of data</td><td valign="top">The Excel Loader assumes data begins in row three. If there are not at least three rows, it means we do not have any data.</td></tr><tr><td valign="top">Errors exist in the input file</td><td valign="top">This is a catch-all for any errors that result from the loader trying to parse input which has not been specified above. Most common cause due to question ID greater than 19 chars.</td></tr><tr><td valign="top">Unable to complete ... Please retry</td><td valign="top">This is a catchall error when the loader is not able to recognize the incoming input info as the HTTP stream it expects. User should not really see this message unless there are network problems with the transmission, old browser, etc.</td></tr><tr><td valign="top">Page cannot be displayed</td><td valign="top">This is a general error usually displayed when there is a connection problem.</td></tr><tr><td valign="top">Script Runtime Error</td><td valign="top">This message is related to a script running (uploading or downloading data). Usually the problem is due to the data or file, but occasionally there are changes made to a script which could also cause this error message.</td></tr><tr><td valign="top">Bulk Insert Conversion Error</td><td valign="top">This error indicates that you are trying to load a response that is longer than 3000 characters. 3000 characters is the limit in Ascribe. If the response is longer, you can cut and paste the rest into another field (notes, transcription, translation).</td></tr><tr><td valign="top">Unable to perform requested action. The error returned is: A temporary file could not be created.</td><td valign="top">We have seen this error when loading a file. This message does not necessarily mean there is something wrong with the file. If you wait and resubmit the same file later, it should work.</td></tr><tr><td valign="top">File is not loaded completely.</td><td valign="top">We have seen this error when trying to download from Ascribe (particularly the IVR Production reports). The error message pops up and when you click “OK” the report is complete. The data for the actual report is present but a lot of extra space is saved in the file.</td></tr><tr><td valign="top">Unable to create the requested file. Make sure the file name is valid and does not contain any illegal characters such as slashes, wild card symbols, etc…</td><td valign="top">This is a generic error message. We believe it is due to the Sequel Server being too busy. If you try the same action again a little later you do not receive an error.</td></tr><tr><td valign="top">Object reference not set to an instance of an object. There is no &#x3C; CBCkey> tag with value 75748103. This value is referenced in &#x3C; DCCBCKey> tag. Study restore error on line 42572xml document.</td><td valign="top">This means there is something in Ascribe that is missing (a translation, code, etc.). Most often the case is that a respondent has a code applied to it from another code book. If this is the case, then if you save and restore the study it will fail and you should be able to use the error message to locate the respondent ID.</td></tr><tr><td valign="top">The source file you entered could not be found.</td><td valign="top">This is a load error. In the past it has turned out to not be a problem from the client’s end.</td></tr><tr><td valign="top"><p>When in the Coder Window:</p><p>Microsoft VBScript runtime error '800a0005'</p><p>Invalid procedure call or argument: ' vbp.PreprocessCodes'</p><p>/Ascribe/Coding/ FrameResponseOpen.asp, line 244</p></td><td valign="top">There are irregular characters in the expressions for this question.</td></tr><tr><td valign="top">When trying to delete a code:<br>DELETE statement conflicted with COLUMN REFERENCE constraint</td><td valign="top">This message is a result of attempting to delete a code from a codebook while that code is still applied to one response.</td></tr><tr><td valign="top"><p>When downloading scripted output “DP Codebook”:</p><p>Script error in line 110. Runtime error 5 in script engine procedure.</p></td><td valign="top">The problem is that there are two Unicode characters in the codebook. Once these two characters are removed the output worked fine.</td></tr><tr><td valign="top">Microsoft VBScript runtime error '800a000d' Type mismatch: ' ele.text' /Ascribe/Client/ SubmitUserDefined.asp, line 91</td><td valign="top"><p>This error occurs when running user defined output when:</p><p>1) "My settings" is selected for the definition of the output template</p><p>2) The user has never entered any values for "My settings"</p><p>To correct the problem, have the user specify something by clicking the Edit button for My Settings.</p></td></tr><tr><td valign="top">Error creating .DAT file. The contents of this file are invalid.</td><td valign="top"><p>This error usually means that there are problems with the Card/Column setup.</p><p>Try running the Card Layout Report to find errors.</p></td></tr><tr><td valign="top">Execution failed: Item has already been added. Key in dictionary: ""Key being added: ""</td><td valign="top">This error means that there are at least two questions with blank QID’s. Giving these questions ID’s should resolve the problem. Usually we see this when a user is trying to run Max Codes Report.</td></tr><tr><td valign="top">Unable to create stream writer for ASCII file</td><td valign="top">This comes up when you are trying to run a scripted output and one or more of the questions has an odd character in it.</td></tr></tbody></table>
